import React from 'react';
import PropTypes from 'prop-types';
import './bio.scss';

const Bio = () => (
  <div className="bio" data-testid="Bio">
    Bio Component
  </div>
);

Bio.propTypes = {};

Bio.defaultProps = {};

export default Bio;
